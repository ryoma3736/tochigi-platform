import '@testing-library/jest-dom';
import { beforeAll, afterAll, afterEach } from 'vitest';

// テスト環境のセットアップ
beforeAll(() => {
  // グローバルなモックやセットアップが必要な場合はここに記述
});

afterEach(() => {
  // 各テスト後のクリーンアップ
});

afterAll(() => {
  // すべてのテスト終了後のクリーンアップ
});
